name: yomu_ui
description: Provisional Flutter UI shell and tokens (replaceable by final design).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: '>=3.32.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  yomu_core:
    path: ../yomu_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
