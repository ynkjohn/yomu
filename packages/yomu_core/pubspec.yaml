name: yomu_core
description: Yomu domain entities, ports and use cases.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  meta: ^1.16.0
  collection: ^1.19.1

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
